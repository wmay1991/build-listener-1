var BuildListener = require('../app/buildListener');

describe("Build Listener Integration", function() {

  describe("will handle http request", function() {

    it("will delegate to buildState and boardController", function() {
      expect(1).toEqual(0);
    });

  });
});
